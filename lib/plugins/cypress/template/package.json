{
  "name": "@monorepo/cypress",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "(cd ../web && yarn build) && concurrently \"PORT=1234 serve -s ../web/build\" \"cypress open\""
  },
  "devDependencies": {
    "concurrently": "^4.1.1",
    "cypress": "^3.4.1",
    "serve": "^11.1.0"
  }
}
